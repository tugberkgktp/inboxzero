# Base URL of the InboxZero API. Exposed to the browser (NEXT_PUBLIC_ prefix).
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:4000
# Production (Vercel): set to your deployed/tunnelled backend URL, e.g.
# NEXT_PUBLIC_API_URL=https://your-tunnel.trycloudflare.com
